Critical gaps (machine is still semi-manual without these)
1. Auto-fiducial search
Currently the operator must manually jog the camera over each fiducial before the servo takes over. The app knows the fiducial design coordinates and the approximate board origin — it should move the camera to the expected fiducial position automatically, then let the servo centre it. This is the single biggest remaining manual step.

2. Machine homing on startup
No homing sequence is enforced before a job. If the machine is powered on mid-travel or after an e-stop, the coordinate system is wrong. The app should send G28 on serial connect (or before job start) and block the job until homing is confirmed.

3. Z-axis surface probing
Dispense height is set manually as a fixed number. The sharpness score from the Python server is already computed but never used. The machine should probe the PCB surface (via G38.2 or a BLTouch-style probe) at job start, set Z=0 at the actual board surface, then apply the configured dispense height on top. Without this, warped boards get inconsistent dots.

4. Board presence detection
Nothing confirms a board is actually loaded before the job starts. A simple camera-based check (look for the expected board outline or brightness pattern in the field of view) or a hardware input (photoelectric sensor on the fixture) would prevent dry-runs on an empty bed.

Hardware items that need software integration
5. Ring light PWM control
Inconsistent illumination is the root cause of most Hough/Otsu detection failures. If the machine has a ring light (or can have one added), the app should control its brightness via a G-code M42 pin or a serial command. Consistent lighting = reliable vision = fewer jogs per fiducial.

6. Actual pressure feedback
Right now PSI is a commanded value — the app has no idea what pressure was actually delivered. A cheap analog pressure transducer on the syringe line, read via an ADC pin on the controller, would close the loop. The app could then flag pads where commanded vs actual pressure diverged.

7. Glue stock by weight
The current stock model is purely mathematical (volume consumed). A load cell under the syringe holder gives ground truth. The software side is mostly ready — GlueStore just needs a readActualWeight() source instead of the computed estimate.

8. Nozzle tip camera / side camera
A small fixed camera aimed at the nozzle tip (not the PCB) could detect clogging or dried glue before it ruins pads. The Python server already has the frame pipeline — adding a second camera index and a /api/nozzle_status endpoint is straightforward.

Workflow gaps (production-readiness)
9. Recipe save/load
All the settings — pressure, dwell, speed, height, bead threshold, purge duration — live in component state and reset on refresh. A simple JSON recipe system (save current settings to a named file, load it back) would let operators switch between glue types and board types without manual reconfiguration.

10. Job queue
For panel production you want to load N boards sequentially without operator interaction. The app handles one panel per job. A queue that reuses the same transform for identical boards (common in SMT lines) would enable lights-out batch runs.

11. SPC / dot quality trend
Dot verification data is collected per-pad but discarded after the job (only the CSV captures it). Aggregating dot diameter and pass/fail rate across jobs would show nozzle wear (dots shrink as the tip clogs) and let the app trigger a mandatory purge before quality falls below spec.

12. Soft axis limits enforcement
Before any G1 move the app should check that the target coordinate is inside the machine's travel envelope. Currently nothing prevents sending a move that would crash into the frame. This is especially important for the vision-snap micro-jogs.






FOR RING LIGHT - 
Option A — Implement software-only camera exposure/gain control (works right now)
The Python server already owns the camera via OpenCV. We can expose CAP_PROP_EXPOSURE, CAP_PROP_BRIGHTNESS and CAP_PROP_GAIN controls via a new API endpoint + sliders in CameraPanel. This achieves the same practical goal — consistent illumination for fiducial detection — purely in software, no hardware needed.

Option B — Stub the UI now, wire hardware later
Add the M42 brightness slider to the settings UI but disable it with a "No ring light connected" note. When you add the hardware, just enable it.










Gaps that matter in production — prioritized:

Priority 1 — Functionally critical (will fail without these)

Gap	Why it matters
Multi-dot pattern for large ICs	QFP/SOP/SOIC with body >5mm needs 2–4 dots to hold. One dot in the center is not enough — component tilts or falls off during reflow
DNP / component exclude list	CSV always has through-hole parts, connectors, large caps that must NOT get glue. No way to skip them currently
Nozzle clog auto-pause	If nozzle clogs mid-job, machine keeps moving but no glue dispenses. Currently detected only after-the-fact by SPC
Priority 2 — Quality/repeatabilityz

Gap	Why it matters
Z-height auto-calibration per-board	Fixed dispense height assumes perfectly flat PCB. Warped boards cause inconsistent dot size
Resume by RefDes in PnP mode	Current resume is by pad index. In PnP mode the operator needs to see "resumed from C8" not "pad 14"
Production batch report	After N boards, show total volume used, pass rate, avg dot diameter, time per board
Priority 3 — Production workflow

Gap	Why it matters
Component height profile	Some components already placed before glue pass need Z-clearance. Need per-footprint safe Z
GCode dry-run preview	Trace the machine path on screen before committing — catches transform errors before wasting glue











To build the Pi installer from your Windows machine:

npm run build:pi
Output lands in dist/ — you'll get a .deb and an .AppImage, both for arm64.

To deploy on the Pi (once you have one):


# 1. Copy the .deb over (USB or SSH)
scp "Glue Dispenser_0.0.0_arm64.deb" pi@raspberrypi.local:~

# 2. Install it
sudo dpkg -i "Glue Dispenser_0.0.0_arm64.deb"

# 3. Allow serial port access (one-time)
sudo usermod -aG dialout pi

# 4. Install the systemd service for auto-start on boot
sudo cp /opt/Glue\ Dispenser/resources/glue-dispenser.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable glue-dispenser
sudo systemctl start glue-dispenser

Ans- No, those commands are Linux terminal commands to be run on the Raspberry Pi itself (via SSH or directly on the Pi's terminal), not on your Windows machine.

Here's the split clearly:

On your Windows machine (now):


npm run build:pi
This creates the .deb and .AppImage files in your dist/ folder.

On the Raspberry Pi (later, when you have it):

Everything else — scp, dpkg, usermod, systemctl — all of that runs on the Pi's Linux terminal. You can either:

Connect a keyboard + monitor directly to the Pi, or
SSH into it from Windows: ssh pi@raspberrypi.local
So your workflow when you get a Pi will be:

Run npm run build:pi on Windows → produces the installer
Copy the .deb file to a USB stick or transfer via network
On the Pi, install it with dpkg and set up the service






/* ------------LOCK TRANSFORM METHOD --------------*/

Normal flow every job:

Load PCB → machine moves camera to each fiducial marker → detects them → solves the transform → starts dispensing
That fiducial detection step takes 30–60 seconds per board.

When Lock Transform is useful:
If the operator uses a precision fixture (a jig with pins/alignment stops that holds every PCB in exactly the same position every time), the board never moves between jobs. The transform from job 1 is still perfectly valid for job 2, 3, 4…

So without Lock Transform: detecting fiducials 100 times/day × 45 seconds = 75 minutes wasted.

With Lock Transform: detect once, lock it, skip detection for all subsequent boards → machine goes straight to dispensing.

The risk: if someone bumps the fixture or places the board slightly off-center, the machine dispenses at wrong positions with no warning — because it trusts the old transform blindly.

Bottom line for your app: if your operator is using a fixture (most production setups do), Lock Transform is a real time-saver. If they're just placing boards by hand each time, it's useless and potentially dangerous.








/*--------- BOARD QUEUE -------------*/
Next up is the board queue — the operator loads N boards upfront and the app runs them one-by-one automatically, prompting between boards instead of going back to idle each time.

Current flow: job finishes → "Job Complete" → operator clicks "Run Next Board" → goes back through preflight → loads PCB → starts again. Every board requires manual navigation.

Queue flow: operator sets "run 10 boards" → job finishes → app automatically advances to "Load PCB for board 2/10" → operator places board, confirms → job starts immediately. No preflight re-run, no navigation.









How MOSFET-Based Glue Dispensing Works
Concept
Pressurized glue sits in a syringe/cartridge. A solenoid valve between the cartridge and needle opens/closes to release glue. The MOSFET is the electronic switch that drives the solenoid — the motion controller can't drive a solenoid directly (too much current).


[Air pressure regulator]
        ↓
[Pressurized glue cartridge]
        ↓
[Solenoid valve] ←── MOSFET ←── Motion controller output pin
        ↓
[Dispensing needle]
        ↓
[PCB pad]
Hardware Required
Part	Purpose
N-channel MOSFET (e.g. IRLZ44N, IRF540N)	Switches solenoid on/off
Flyback diode (e.g. 1N4007)	Protects MOSFET from solenoid kickback
10kΩ resistor (gate-to-ground)	Prevents floating gate
100Ω resistor (gate series)	Limits gate current
12V or 24V solenoid valve	Controls glue flow
12V/24V power supply	Powers the solenoid
Pressurized glue cartridge + regulator	Stores and pressures the glue
Wiring Circuit

Motion controller pin ──[100Ω]──┬── MOSFET Gate
                                │
                               [10kΩ]
                                │
                               GND

MOSFET Drain ──── Solenoid (−)
MOSFET Source ─── GND

Solenoid (+) ──── 12V/24V supply (+)

[1N4007 diode across solenoid: Anode to Drain, Cathode to 12V supply]
Which Pin on the Motion Controller
On GRBL-based boards:

Fan pin → M106 (ON) / M107 (OFF) — easiest, no config needed
Spindle pin → M3 S255 (ON) / M5 (OFF)
Any aux pin → M42 P[n] S255 (ON) / M42 P[n] S0 (OFF)
Fan pin is recommended — it's already broken out on most GRBL boards and needs no firmware changes.

G-code Dispense Sequence Per Pad

G0 X10.5 Y23.2        ; Move to pad position
G0 Z1.0               ; Lower to dispense height
M106                  ; Solenoid ON → glue starts flowing
G4 P150               ; Dwell 150ms → this is your "dwell time"
M107                  ; Solenoid OFF → glue stops
G0 Z5.0               ; Raise Z (safe travel)
; repeat for next pad
The dwell time (ms) = glue volume. Longer pulse = more glue.

Air Pressure Role
Air pressure is set mechanically on the regulator (e.g. 2–5 PSI for thin glue, 10–30 PSI for thick)
Higher pressure = more glue per ms → you compensate by reducing dwell time
The app's "pressure PSI" value is currently used for per-pad dwell calculation (larger pads get longer dwell automatically). The actual air pressure is a physical knob on the regulator.
App Operation (Current State)
The app already has:

Dispense Pressure (PSI) — set this to match your regulator reading; the sequencer uses it to scale dwell times per pad size
Base Dwell Time (ms) — starting point; tune this for your glue viscosity + air pressure combination
Live pad card — shows PSI / dwell / µL for each pad as it dispenses
Post-job summary — total glue used, duration
What the app currently sends for dispense depends on what dispensePoint() in gcode.js generates. I need to check whether it already sends M106/M107 or just a G4 dwell without solenoid control — that's the key question before we change anything.

Calibration Procedure
Set regulator to target pressure (e.g. 20 PSI)
Place paper/PCB under needle
Run a single-pad test with dwell = 100ms
Measure dot size → adjust dwell up/down
Repeat until dot matches pad size
Save that dwell as Base Dwell Time in the app
What Needs Checking Before We Implement
The critical question: does dispensePoint() in gcode.js currently send M106/M107 around the G4 dwell, or does it only send G4? If solenoid control G-code isn't there yet, that's the one thing to add. Want me to check and show you what it currently does?












1. manually set pressure, time/mode, vaccuum on 983A machine.
2. manual set base dwell, z work height, z safe height, pressure on the app.

To Get Live PSI (Optional Hardware Add-On) :-
               If you want live pressure in the app, add a 0–10 bar analog pressure transducer (INR 300–800) to the air line between compressor and 983A. Wire its 0–5V output to an analog input pin on the CNC board. Marlin reads it via M105 or a custom query. I can implement this if you get the sensor.





The setup becomes:

3D Printer Controller
        |
        |
   GPIO Output
        |
     MOSFET
        |
        |
983A Foot Switch Input

When the GPIO turns ON:

MOSFET ON
   ↓
Foot switch shorted
   ↓
983A starts dispensing

When the GPIO turns OFF:

MOSFET OFF
   ↓
Foot switch open
   ↓
983A stops dispensing








Your Immediate Action
Before writing any more code: check your CNC board to find which physical pin corresponds to the M42 command. You need to know:

What board is it (RAMPS 1.4, SKR, MKS, etc.)?
What is the physical connector for "Fan 0", "Servo", or "AUX" pins?
That pin is what you wire to the 983A trigger input. The code is already correct — the missing piece is just the physical wire.









What's Actually Happening
Step 1: App sends G28 (home all axes) at connect.

Step 2: Marlin homes X successfully → then tries to home Y.

Step 3: Y stepper driver is not working (not seated, missing, or no endstop) → Y motor never moves → G28 hangs forever waiting for Y endstop to trigger.

Step 4: You click jog → app sends G1 X10 → Marlin queues it behind the stuck G28 → machine stays frozen → nothing moves.

The jog buttons aren't broken — their commands are just stuck in the queue behind the never-finishing G28.

Fix 1 — Hardware (permanent, correct fix)
On your RAMPS 1.4 board, check that all three stepper driver modules are properly seated:

Slot	Driver needed
X	A4988 or DRV8825
Y	A4988 or DRV8825
Z	A4988 or DRV8825
A4988/DRV8825 chips need to be pressed firmly onto the 8-pin headers. A half-seated chip gives no motion. Also verify Y and Z endstop cables are connected to Y-MIN and Z-MIN on the RAMPS board.

What You Still Need to Fix on RAMPS Hardware
#	Check	What to look for
1	Y stepper driver	Is an A4988/DRV8825 firmly seated in the Y slot?
2	Z stepper driver	Is an A4988/DRV8825 firmly seated in the Z slot?
3	Y endstop cable	Is it plugged into Y-MIN on RAMPS?
4	Z endstop cable	Is it plugged into Z-MIN on RAMPS?
Once all three drivers are seated and all three endstops are wired, change the code back from G28 X to G28 to home all axes on connect.




/* ============================================================
   FEATURES IMPLEMENTED IN SOFTWARE (Session Log)
   ============================================================ */


/* ---- 1. skipHome Bug Fix (src/App.jsx) ---- */

Problem:
After the very first successful connect, hasConnectedRef.current was permanently set to true.
skipHome={hasConnectedRef.current} was therefore always true after the first session,
meaning G28 was never sent again even after a full disconnect and reconnect.
A secondary bug also prematurely called setIsHomed(true) on reconnect before G28 had run.

Fix applied:
- Changed skipHome={hasConnectedRef.current} → skipHome={isHomed}
- isHomed is reset to false on every disconnect (both intentional and cable-pull)
- onConnect callback now always calls setIsHomed(false); only onHomingComplete sets it true
- Result: G28 runs on every connect. isHomed only becomes true when homing physically completes.

Files changed: src/App.jsx


/* ---- 2. Persistent Fault Log (src/Toast.jsx + electron/main.js + electron/preload.js) ---- */

Problem:
All errors and warnings appeared as temporary toast messages that disappeared after 4–5 seconds.
No permanent record of faults existed. In a production environment this means:
- No audit trail of what went wrong and when
- Faults during unattended runs are invisible
- No data for maintenance decisions

Implementation:
- Every toast.error() and toast.warning() call anywhere in the app now automatically
  writes a row to Documents\GlueJobLogs\fault-log.csv
- CSV format: Timestamp (ISO 8601), Level (ERROR / WARNING), Message
- Logging is wired at the ToastProvider level — no changes needed in any component
- If the log file does not exist, it is created with a header row automatically
- Logging is fire-and-forget (.catch(() => {}) so a disk error never blocks the UI

IPC handler added: fs:appendFaultLog (electron/main.js)
Preload method added: window.fs.appendFaultLog (electron/preload.js)
Files changed: src/Toast.jsx, electron/main.js, electron/preload.js

Log location: C:\Users\<user>\Documents\GlueJobLogs\fault-log.csv


/* ---- 3. User Access Levels — Admin / Operator PIN Lock ---- */
/*         (src/components/AppHeader.jsx, src/App.jsx,          */
/*          src/components/AutomatedDispensingPanel.jsx)         */

Problem:
Any operator could accidentally change critical dispensing parameters (pressure, dwell time,
Z heights, valve commands, axis limits) and corrupt the calibrated process settings.
No distinction existed between an operator running jobs and a technician changing settings.

Implementation:
- A lock/unlock button appears in the top header bar at all times
- Two modes:
    OPERATOR mode (default, 🔒 OPERATOR — yellow):
      Operator can connect/disconnect, run jobs, view stats, load recipes, view all data.
      All dispensing parameter inputs (pressure, dwell, Z heights, valve commands, axis limits,
      bead settings, PWM duty, etc.) are covered by a locked overlay showing:
      "🔒 Settings locked — Log in as Admin to change parameters"
      The overlay uses CSS pointer-events so inputs cannot be clicked through it.
    ADMIN mode (🔓 ADMIN — green):
      Full access to all settings. Unlocked by entering the admin PIN.

- Default PIN: 1234
- PIN is stored in localStorage under key 'glueAdminPin'
- To change the PIN: open browser DevTools console and run:
    localStorage.setItem('glueAdminPin', 'yournewpin')
- Clicking the lock button while in Admin mode immediately locks back to Operator
- Clicking the lock button while in Operator mode shows a prompt for the PIN

Files changed: src/components/AppHeader.jsx, src/App.jsx, src/components/AutomatedDispensingPanel.jsx


/* ---- 4. Recipe System (already existed — no new implementation needed) ---- */

Status: Already fully implemented before this session.
- Recipes stored in localStorage under key 'glueRecipes'
- Parameters saved: pressure, dwell, dispense Z, safe travel Z, viscosity,
  bead threshold, bead feed rate, purge on/off, purge duration, valve commands,
  dot verification, surface probe, nozzle diameter
- UI: Recipe Manager section in Automated Dispensing panel (collapsible)
- Functions: Save As (named recipe), Load, Delete, Export to JSON file, Import from JSON file
- Active recipe name shown in the Recipe Manager header

Additionally this session added file-based IPC handlers for recipes (for future use
if recipes need to survive across app reinstalls or be shared between machines):
  window.fs.saveRecipe(name, data)   → Documents\GlueRecipes\<name>.json
  window.fs.loadRecipes()            → returns all recipes as an array
  window.fs.deleteRecipe(name)       → deletes the named recipe file

Files changed (IPC only): electron/main.js, electron/preload.js


/* ---- 5. pauseSerialPolling Race Condition Fix ---- */
/*         (src/main.jsx, src/components/SerialPanel.jsx)        */

Problem:
window.pauseSerialPolling was a plain boolean. Multiple code paths (homing in SerialPanel
and job execution in AutomatedDispensingPanel) set and cleared it independently.
The critical failure scenario:
  1. Connect → homing starts → pauseSerialPolling = true
  2. Cable is pulled during homing
  3. onDisconnect fires → clears isHoming state but does NOT clear pauseSerialPolling
  4. Reconnect → new homing sets pauseSerialPolling = true (already true — no change)
  5. Homing completes → pauseSerialPolling = false
  6. But if a second consumer (job execution) had also set it to true, that consumer's
     false-clear now incorrectly un-pauses polling while the job is still running
  7. M114 responses flood the ackQueue during job execution → job sends wrong G-code

This caused the "jog doesn't work after failed homing" symptom.

Fix: Reference-counted pause system
- window._pollPauseCount = 0 (counter initialized in src/main.jsx before React renders)
- window.pauseSerialPolling is now a computed property:
    getter: returns (_pollPauseCount > 0)
    setter: true → increments counter; false → decrements but never below 0
- All existing call sites (window.pauseSerialPolling = true/false) work unchanged
- onDisconnect now resets window._pollPauseCount = 0 (clean slate on cable pull)
- connect() also resets window._pollPauseCount = 0 at the start (clean slate on reconnect)

Effect: Multiple concurrent pausers (homing + job) can independently pause/resume
polling without accidentally clearing each other. Cable pull during any operation
always recovers cleanly on the next connect.

Files changed: src/main.jsx, src/components/SerialPanel.jsx


/* ---- 6. Dynamic Boot Timing (src/components/SerialPanel.jsx) ---- */

Problem:
The connect sequence used hardcoded delays:
  - setTimeout 8000ms → start M114 polling
  - setTimeout 9000ms → send G28 (home all axes)
These magic numbers were tuned for Arduino Mega 2560 + RAMPS 1.4 booting in ~6 seconds.
On faster hardware, 8 seconds was wasted. On slower hardware or different firmware,
G28 was sent before Marlin was ready, causing silent failure or timeout.

Fix: Event-driven boot detection with fallback
- A marlinBootCbRef is set immediately after the port opens
- The onData handler watches every incoming serial line for boot indicators:
    "start"           → Marlin's earliest boot signal (sent before firmware initialises)
    any line with "Marlin"  → version string (e.g. "Marlin 2.1.1")
    any line starting with "ok"  → Marlin is definitely alive and accepting commands
- When any boot indicator is received, the callback fires immediately:
    → startStatusQuery() begins M114 polling
    → 500ms settle (clears remaining boot chatter)
    → G28 + M400 sent → homing begins
- Fallback: if no boot message arrives within 12 seconds, proceeds anyway with a
  console warning (covers non-Marlin firmware and very slow boards)
- bootHandled flag prevents double-firing if multiple boot indicators arrive
- marlinBootCbRef is cleared on disconnect so stale callbacks never fire on next connect
- Watchdog grace period updated from 8s → 15s to safely cover the 12s fallback path

Typical result on Mega 2560 + RAMPS:
  Before: always waited 8s before M114, 9s before G28 (regardless of actual boot time)
  After:  homing starts at ~3.5–4s (as soon as Marlin sends its first "ok")

Files changed: src/components/SerialPanel.jsx


/* ---- 7. Marlin Error Detection Mid-Job ---- */
/*         (src/components/AutomatedDispensingPanel.jsx)          */

Problem:
If Marlin sent an error response during a running job (e.g. Error:MAXTEMP,
Error:Printer halted. kill() called!, !!, echo:Unknown command), the job continued
silently. The ackQueue entries waited forever for "ok" responses that would never arrive,
and the machine stayed halted while the app believed it was still dispensing.

Root issue: ackQueue stored bare resolve functions — there was no reject path.
Errors from Marlin had no way to propagate back to sendGcodeWait.

Fix:
- ackQueue entries now store {resolve, reject} pairs instead of bare resolve functions
- handleData detects the following Marlin error patterns:
    /^Error:/i           → all Marlin error conditions (thermal, position, etc.)
    trimmed === '!!'     → emergency halt (thermal runaway, etc.)
    /^echo:Unknown command/i  → unrecognised G-code command
- On detection (only when job is running):
    1. marlinFaultRef.current is set to the error line
    2. All pending ackQueue entries are rejected with Error("Machine fault: <line>")
    3. sendGcodeWait throws, propagating up to runDispenseLoop's catch block
    4. Job stops cleanly with a toast showing the exact Marlin error text
    5. The fault is simultaneously written to fault-log.csv (via Toast layer)
- sendGcodeWait checks marlinFaultRef at the start of every call — refuses to send
  further commands after a fault (prevents flooding a halted machine with G-code)
- marlinFaultRef is cleared at the start of each new job run
- cancelJob now properly rejects pending acks (entry.reject) instead of just
  emptying the array — ensures the loop unwinds immediately on operator abort

Error flow:
  Marlin → "Error:MAXTEMP"
    → handleData detects it
    → all pending acks rejected
    → sendGcodeWait throws "Machine fault: Error:MAXTEMP"
    → runDispenseLoop catch block: toast.error(e.message) + job stops
    → fault-log.csv gets a new row

Files changed: src/components/AutomatedDispensingPanel.jsx


/* ---- 8. Mid-Job Pause / Resume Button ---- */
/*         (src/components/AutomatedDispensingPanel.jsx)          */

Problem:
The only way to stop a running job was to abort it entirely. If an operator needed to
check the nozzle, swap a syringe, or briefly pause production, they had to abort and
lose their place — then manually set resumeFromPad to continue.

Implementation:
- New state: isOperatorPaused (React state for UI), operatorPausedRef (ref for loop access)
- New functions:
    pauseJob()          → sets operatorPausedRef true + shows info toast
    resumeOperatorPause() → clears operatorPausedRef
- Pause check inserted at the top of each pad iteration in runDispenseLoop:
    if (operatorPausedRef.current) {
      while (operatorPausedRef.current && isJobRunningRef.current) {
        await new Promise(r => setTimeout(r, 300));  // poll every 300ms
      }
      if (!isJobRunningRef.current) throw new Error("Job Aborted");
    }
- The check is deliberately BETWEEN pads — the machine always finishes the current
  pad fully before pausing. Glue is never left partially applied.
- cancelJob() clears operatorPausedRef before setting isJobRunningRef false,
  so an abort while paused propagates cleanly without deadlock.

UI changes:
- ⏸ PAUSE button: appears alongside ABORT JOB only while a job is running
  When paused → changes to ▶ RESUME (highlighted in blue)
- Yellow status banner displayed below the buttons while paused:
  "⏸ Job paused — machine will hold after current pad. Click RESUME to continue or ABORT to stop."
- Download G-Code button is hidden while job is running (was always disabled anyway)

States:
  Job running, not paused   → shows [⏹ ABORT JOB] [⏸ PAUSE] buttons
  Job running, paused       → shows [⏹ ABORT JOB] [▶ RESUME] buttons + yellow banner
  Job not running           → shows [▶ START JOB] button only

Files changed: src/components/AutomatedDispensingPanel.jsx






G-code Flow — Source to Machine

UI Component
    │
    ▼
src/lib/motion/gcode.js        ← builds G-code strings
    │
    ▼
window.serial.writeLine()      ← IPC call (defined in preload.js)
    │
    ▼
electron/preload.js            ← bridges renderer → main process
    │
    ▼
electron/main.js               ← writes to SerialPort (USB)
    │
    ▼
RAMPS / Marlin firmware












Wiring — Arduino Mega Pins to 2DM556
The simplest approach: use the same pin numbers that RAMPS uses so you don't need to change any Marlin pin configuration. Just wire those Arduino Mega pins directly to each 2DM556.

Signal	             Arduino Mega Pin	           Wire to 2DM556
X STEP	               Pin D2 	                  PUL+ (via 1kΩ resistor)
X DIR	                 Pin D5 	                  DIR+ (via 1kΩ resistor)
X ENABLE	             Pin D8	                    ENA+ (via 1kΩ resistor)
Y STEP	               Pin D3	                    PUL+
Y DIR	                 Pin D6	                    DIR+
Y ENABLE	             Pin D9	                    ENA+
Z STEP	               Pin D4	                    PUL+
Z DIR	                 Pin D7	                    DIR+
Z ENABLE	             Pin D10                     ENA+
X limit switch	       Pin D22	                  signal wire
Y limit switch	       Pin D23	                  signal wire
Z limit switch	       Pin D24	                  signal wire
All PUL−, DIR−, ENA− → common GND (Arduino GND and power supply GND joined)


Arduino Mega Pin 54 ──[1kΩ]── PUL+
Arduino GND ──────────────── PUL−
                              2DM556
Arduino Mega Pin 55 ──[1kΩ]── DIR+
Arduino GND ──────────────── DIR−

Arduino Mega Pin 38 ──[1kΩ]── ENA+
Arduino GND ──────────────── ENA−
The 1kΩ resistors protect the optocouplers inside the 2DM556 at 5V logic.

Marlin Configuration.h Changes
1. Change the board type:


// Was:
#define MOTHERBOARD BOARD_RAMPS_14_EFB

// Change to:
#define MOTHERBOARD BOARD_MEGA
2. Remove A4988 driver type definitions:


// Remove or comment out:
// #define X_DRIVER_TYPE  A4988
// #define Y_DRIVER_TYPE  A4988
// #define Z_DRIVER_TYPE  A4988
2DM556 is a plain external driver — Marlin doesn't control it over SPI/UART. Leave driver type undefined (or NONE).

3. Fix enable pin polarity — CRITICAL:


// 2DM556 enables motor when ENA+ is HIGH
// So Marlin must set enable pin HIGH to activate
#define X_ENABLE_ON 1
#define Y_ENABLE_ON 1
#define Z_ENABLE_ON 1
(RAMPS with A4988 uses 0 = LOW to enable. 2DM556 is opposite.)

4. Recalculate steps per unit based on 2DM556 DIP switches:

Check your 2DM556 DIP switches. The microstepping is set hardware-side:

2DM556 DIP setting	Microsteps	Steps/mm (2mm lead screw)	Steps/mm (4mm lead screw)
400 pulse/rev	2	200	100
800 pulse/rev	4	400	200
1600 pulse/rev	8	800	400
3200 pulse/rev	16	1600	800
6400 pulse/rev	32	3200	1600
Formula: steps/mm = (pulse_per_rev) ÷ (lead_screw_pitch_mm)


// Example: 1600 pulse/rev DIP setting, 2mm pitch lead screw
#define DEFAULT_AXIS_STEPS_PER_UNIT { 800, 800, 800, 93 }
5. Disable internal endstop pullups (2DM556 setup usually means external wiring):


// Check if your endstops need pullups — if wired with external resistors, disable:
// #define ENDSTOPPULLUPS
#define ENDSTOPPULLUP_XMIN
#define ENDSTOPPULLUP_YMIN
#define ENDSTOPPULLUP_ZMIN
Power Supply
The 2DM556 needs a separate DC power supply for motors (24V–48V, minimum 5A per driver). Arduino Mega runs from USB or its own 5V/12V barrel jack. Join GND of motor PSU with Arduino GND — this is mandatory for signal reference.

After reflashing Marlin with these changes, run M119 to confirm endstops, then G28 to test homing.






Method: Calculate pitch from Marlin (if the machine already moves correctly)

If you know:

Motor = 200 steps/rev
Microstepping = 16
Marlin Steps/mm = 80

Then:

Lead=
Steps/mm
Motor Steps×Microsteps
	​
